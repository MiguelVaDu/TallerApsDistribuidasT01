# --- Server Configuration ---
server.port=8080

# --- Application Identification ---
spring.application.name=api-gateway

# --- Eureka Client Configuration ---
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# --- Spring Cloud Gateway Route Configuration ---

# Ruta para Product Service
# Cualquier petición al Gateway que empiece con /api/products/** será redirigida a una instancia de PRODUCT-SERVICE
spring.cloud.gateway.routes[0].id=product-service-route
spring.cloud.gateway.routes[0].uri=lb://PRODUCT-SERVICE
spring.cloud.gateway.routes[0].predicates[0]=Path=/api/products/**
# Opcional: Si necesitas reescribir paths o añadir/quitar prefijos:
# spring.cloud.gateway.routes[0].filters[0]=RewritePath=/api/products/(?<segment>.*), /api/products/$\{segment}
# En este caso, si el path es el mismo, no se necesita RewritePath.

# Ruta para Order Service
spring.cloud.gateway.routes[1].id=order-service-route
spring.cloud.gateway.routes[1].uri=lb://ORDER-SERVICE
spring.cloud.gateway.routes[1].predicates[0]=Path=/api/orders/**

# Ruta para Customer Service
spring.cloud.gateway.routes[2].id=customer-service-route
spring.cloud.gateway.routes[2].uri=lb://CUSTOMER-SERVICE
spring.cloud.gateway.routes[2].predicates[0]=Path=/api/customers/**

# Ruta para Payment Service
spring.cloud.gateway.routes[3].id=payment-service-route
spring.cloud.gateway.routes[3].uri=lb://PAYMENT-SERVICE
spring.cloud.gateway.routes[3].predicates[0]=Path=/api/payments/**


# --- Opcional: Configuración global para Circuit Breaker (Resilience4j) ---
# Este es un filtro por defecto que se aplicará a todas las rutas.
# spring.cloud.gateway.default-filters[0].name=CircuitBreaker
# spring.cloud.gateway.default-filters[0].args.name=defaultCircuitBreakerConfig
# spring.cloud.gateway.default-filters[0].args.fallbackUri=forward:/fallback-general # Necesitas un controller para este fallback

# resilience4j.circuitbreaker.configs.default.slidingWindowType=COUNT_BASED
# resilience4j.circuitbreaker.configs.default.slidingWindowSize=10
# resilience4j.circuitbreaker.configs.default.failureRateThreshold=50
# resilience4j.circuitbreaker.configs.default.waitDurationInOpenState=10s
# resilience4j.circuitbreaker.instances.defaultCircuitBreakerConfig.baseConfig=default

# --- Actuator Endpoints (para Gateway) ---
management.endpoints.web.exposure.include=gateway,health,info,metrics